# ChatGPT2API 本地启动指南（不使用 Docker）

## 📋 前置要求

### 1. 安装 Python 3.13+

**下载地址：** https://www.python.org/downloads/

安装时**务必勾选** "Add Python to PATH"

安装完成后，打开新的命令行窗口验证：
```cmd
python --version
```

### 2. 安装 Node.js（已安装 ✅）

你已经安装了 Node.js v24.15.0

---

## 🚀 启动步骤

### 第一步：安装后端依赖

打开命令行，进入项目目录：
```cmd
cd d:\chatgpt2api
```

安装 Python 依赖：
```cmd
pip install curl-cffi fastapi pillow pybase64 python-multipart tiktoken uvicorn sqlalchemy psycopg2-binary gitpython
```

如果 pip 速度慢，可以使用国内镜像：
```cmd
pip install -i https://mirrors.aliyun.com/pypi/simple/ curl-cffi fastapi pillow pybase64 python-multipart tiktoken uvicorn sqlalchemy psycopg2-binary gitpython
```

### 第二步：配置认证密钥

编辑 `config.json` 文件，修改 `auth-key`（或保持默认）：
```json
{
  "auth-key": "chatgpt2api"
}
```

### 第三步：启动后端服务

```cmd
python main.py
```

后端将在 **http://localhost:8000** 启动

### 第四步：安装前端依赖

打开**新的命令行窗口**，进入前端目录：
```cmd
cd d:\chatgpt2api\web
```

安装依赖：
```cmd
npm install
```

如果 npm 速度慢，可以使用国内镜像：
```cmd
npm config set registry https://registry.npmmirror.com
npm install
```

### 第五步：启动前端服务

```cmd
npm run dev
```

前端将在 **http://localhost:3000** 启动

---

## 🔑 添加账号

### 方法 1：通过 Web 界面（推荐）

1. 打开浏览器访问：http://localhost:3000
2. 点击左侧菜单 "账号管理"
3. 点击 "添加账号" 按钮
4. 粘贴你的 **access_token**（一行一个）
5. 点击确认

### 方法 2：通过 API 添加

使用 curl 或 Postman：

```bash
curl http://localhost:8000/api/accounts \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer chatgpt2api" \
  -d "{\"tokens\": [\"你的access_token\"]}"
```

Windows PowerShell 版本：
```powershell
$headers = @{
    "Content-Type" = "application/json"
    "Authorization" = "Bearer chatgpt2api"
}
$body = @{
    tokens = @("你的access_token")
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:8000/api/accounts" -Method Post -Headers $headers -Body $body
```

### 方法 3：直接编辑数据文件

在项目根目录创建 `data` 文件夹（如果不存在），然后创建 `data/accounts.json`：

```json
[
  {
    "access_token": "eyJhbGci...",
    "refresh_token": "可选",
    "id_token": "可选",
    "email": "可选",
    "type": "Plus"
  }
]
```

重启后端服务即可加载。

---

## 📝 获取 access_token 的方法

1. 登录 ChatGPT 官网：https://chatgpt.com
2. 打开浏览器开发者工具（F12）
3. 切换到 "应用程序"（Application）或 "存储"（Storage）标签
4. 找到 Cookies → https://chatgpt.com
5. 复制 `__Secure-next-auth.session-token` 的值

或者：

1. 在 ChatGPT 页面打开开发者工具（F12）
2. 切换到 "网络"（Network）标签
3. 刷新页面
4. 找到任意请求，查看请求头中的 `Authorization: Bearer eyJhbGci...`
5. 复制 `Bearer` 后面的 token

---

## ✅ 验证服务

### 检查后端
访问：http://localhost:8000/v1/models

应该返回可用的模型列表。

### 检查前端
访问：http://localhost:3000

应该看到管理界面。

---

## 🔧 常见问题

### 问题 1：端口被占用

如果 8000 或 3000 端口被占用，可以修改：

**后端端口：** 编辑 `main.py`
```python
if __name__ == "__main__":
    uvicorn.run(app, host="0.0.0.0", port=8001, access_log=False, log_level="info")
```

**前端端口：** 编辑 `web/package.json`
```json
"dev": "next dev --webpack -H 0.0.0.0 -p 3001"
```

### 问题 2：Python 依赖安装失败

某些依赖（如 `psycopg2-binary`）可能需要编译环境。如果安装失败：

1. 安装 Microsoft C++ Build Tools
2. 或者跳过该依赖（如果不使用 PostgreSQL）

### 问题 3：前端无法连接后端

检查 `web/src` 目录下的 API 配置文件，确保后端地址正确。

---

## 🎯 使用 API

添加账号后，就可以使用 API 了：

### 文生图示例

```bash
curl http://localhost:8000/v1/images/generations \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer chatgpt2api" \
  -d "{\"model\": \"gpt-image-2\", \"prompt\": \"一只可爱的猫\", \"n\": 1}"
```

### 接入第三方客户端

- **API 地址：** http://localhost:8000/v1
- **API Key：** chatgpt2api（或你在 config.json 中设置的值）
- **支持的模型：** gpt-image-2, codex-gpt-image-2, auto, gpt-5 等

可以接入 Cherry Studio、New API 等客户端。

---

## 📚 更多信息

查看项目 README.md 了解更多功能和配置选项。
